[Unit]
Description=Report a failed graphify refresh to Cloud Logging

[Service]
Type=oneshot

# Runs as root, not as the graphify user: it writes to Cloud Logging using the
# VM service account via gcloud, and needs no access to /data at all.
ExecStart=/usr/local/bin/graphify-log-failure
