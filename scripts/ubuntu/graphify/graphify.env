# graphify service configuration — baked to /etc/graphify/graphify.env.
#
# Sourced by the helper scripts in /usr/local/bin and read by both systemd units
# as an EnvironmentFile. This is the ONE place these values are written; change
# them here and rebuild the image, or override per-VM with a systemd drop-in
# (systemctl edit graphify-refresh.service).
#
# systemd's EnvironmentFile parser does NOT expand variables, so every path is
# spelled out in full rather than composed from the ones above it.

# ---------------------------------------------------------------------------
# Paths. The split matters: the venv is on the BOOT disk (baked, replaceable),
# everything stateful is on the DATA disk (survives an image swap).
# ---------------------------------------------------------------------------
GRAPHIFY_VENV=/opt/graphify/venv
GRAPHIFY_DATA=/data
GRAPHIFY_REPOS=/data/repos

# The service user's HOME. graphify's `global add` writes to
# Path.home()/".graphify" — hardcoded upstream, with no flag to redirect it — so
# HOME must be on the data disk or the merged graph silently lands on the boot
# disk and is lost the next time the image changes.
GRAPHIFY_HOME=/data/graphify
GRAPHIFY_GRAPH=/data/graphify/.graphify/global-graph.json

# Refresh watermark: the UTC timestamp of the last SUCCESSFUL run. Repos whose
# GitHub pushed_at is at or below this are skipped without being fetched.
GRAPHIFY_STATE=/data/graphify/last-run

# ---------------------------------------------------------------------------
# Server. --host must be 0.0.0.0: upstream defaults to 127.0.0.1, which the load
# balancer cannot reach at all. Containment comes from the VM having no external
# IP, the firewall admitting only Google's LB ranges, and the API-key gate — not
# from the bind address.
# ---------------------------------------------------------------------------
GRAPHIFY_PORT=8080
GRAPHIFY_HTTP_PATH=/mcp

# ---------------------------------------------------------------------------
# Secret Manager secret ids, in the VM's own project. Values are never baked;
# they are fetched at run time and kept in process memory.
# ---------------------------------------------------------------------------
GRAPHIFY_API_KEY_SECRET=graphify-mcp-api-key
GRAPHIFY_GITHUB_PAT_SECRET=graphify-github-pat

# ---------------------------------------------------------------------------
# What to index.
#
# Default is every non-archived repo the PAT can see in the org. The exclude list
# is space-separated and holds repos that cost a fetch and a scan but contribute
# almost nothing to the graph: config- and docs-only repos parse to a handful of
# nodes (build-parent-pom yields a single one), and graphify has no HTML or CSS
# grammar, so a static-site repo indexes only its JavaScript.
#
# OPEN ITEM: this list is a starting guess, not a measured one. After the first
# full build, check `graphify global list` for repos contributing near-zero nodes
# and add them here.
# ---------------------------------------------------------------------------
GRAPHIFY_GITHUB_ORG=deployza
GRAPHIFY_EXCLUDE_REPOS="build-docs build-parent-pom"
